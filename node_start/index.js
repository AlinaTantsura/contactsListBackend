// const date = new Date();

// console.log(`Today is ${date.getFullYear}`);

const { getCurrentYear } = require('./getCurrentYear');
const { getCurrentDate } = require("./getCurrentDate");

module.exports = {getCurrentYear, getCurrentDate};